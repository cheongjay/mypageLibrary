import { convert, buildPages } from "./convert.js";
import { createPages } from "./createPages.js";
// makeCard(md 파일, PostCard);
// makeBedge(md 파일, SkillBedge);

convert('profile','result');

// buildPages();
createPages();
